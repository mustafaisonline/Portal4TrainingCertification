# Project Status — START HERE when returning

> **As at 2026-09-26.** Maintained as the single resume point. If this file and any completion report disagree, the completion report is more detailed; if this file and `git log` disagree, `git log` wins — update this file.
> **Branch:** `feat/production-foundation` · **pushed to `origin/feat/production-foundation` on 2026-09-26** (founder direction: "commit and push this to dev" — no `dev` branch exists on this repo, so this branch is the development line; opening the PR to `main` remains the founder's call) · working tree clean (see `git log`).
> **Production: NOTHING IS DEPLOYED.** (Founder-confirmed 2026-09-26.) No production environment exists — no hosting, no production database, no domain, no email provider, no live Stripe keys. **The hosting model IS now decided** (2026-09-26, ADR-046: DigitalOcean Droplet + Managed PostgreSQL + Caddy, through the `deploy/` framework) and the framework is built and rehearsed — but **no DigitalOcean resource, registry, DNS record or live Stripe endpoint exists**; domain (K13) and the residency confirmation (J1) are still open (§3 items 2 and 10). The push above is source control only, not a release: the only running copy of this application is the local dev server (`npm run dev` → `http://localhost:3100`) against the local `p4tc_dev` database. The public GitHub Pages site (`mustafaisonline.github.io/Portal4TrainingCertification`) is the disposable **mockup** in `project-artifacts/mockup`, not this application. Every "verified" or "confirmed" note in this file means verified locally.
> **Read next, in order:** `CLAUDE.md` (constitution) → this file → the plan and completion report of whatever you are about to touch → the specifications only as needed.

## 1. What the product is (one paragraph)
An independent expert-led **training and certification organisation** (DR-02). The portal supports that ecosystem: public programme pages, scheduled offerings, Stripe registration, participant accounts and profiles, learner Reviews, a **Certificate of Completion** with public verification and yearly renewal, and administrator operations. The **earned credential** (DR-01: one credential, evidence-based) is a separate, later product surface — nothing here is it, and every certificate page says so.

## 2. Where the build stands
All ten roadmap milestones of [`WIREFRAME_TO_PRODUCTION_PLAN.md`](WIREFRAME_TO_PRODUCTION_PLAN.md) §7 have been executed as far as the founder's approvals allow.

| Milestone | State | Commits | Report |
|---|---|---|---|
| M1 Walking skeleton · M1b | done | early history | M1 report |
| M2 Identity & access (Better Auth; no email verification; password ≥ 8; MFA removed for MVP 1) | done | | [`MILESTONE_2_COMPLETION_REPORT.md`](MILESTONE_2_COMPLETION_REPORT.md) |
| M3 Catalogue & public portal | done | | [`MILESTONE_3_COMPLETION_REPORT.md`](MILESTONE_3_COMPLETION_REPORT.md) |
| M4 Registration & payment (Stripe cards; refund tiers 100/50/0 net of Stripe fee; one free transfer; price by profile country; legal DRAFTS) | done, real test-mode payment and refund verified | | [`MILESTONE_4_COMPLETION_REPORT.md`](MILESTONE_4_COMPLETION_REPORT.md) |
| M5a User profile (email immutable login; ID encrypted; photo; checkout gate) | done | `3dd0e60` `4266481` | [`MILESTONE_5A_COMPLETION_REPORT.md`](MILESTONE_5A_COMPLETION_REPORT.md) |
| M5b Reviews (mandatory per certificate; moderation; consent-default-private) | done | `8cc5856` `bd94a97` `8a05493` | [`MILESTONE_5B_COMPLETION_REPORT.md`](MILESTONE_5B_COMPLETION_REPORT.md) |
| M6 Certificate of Completion & verification (E1–E14 approved) | done | `5c05236` `dd44e32` `2100f32` | [`MILESTONE_6_COMPLETION_REPORT.md`](MILESTONE_6_COMPLETION_REPORT.md) |
| M7 Renewal reminders | done (no schema change) | `9bacb37` | [`MILESTONE_7_COMPLETION_REPORT.md`](MILESTONE_7_COMPLETION_REPORT.md) |
| M8 Admin operations | done on the existing schema · **§5 awaits approval** | `a2b820d` | [`MILESTONE_8_COMPLETION_REPORT.md`](MILESTONE_8_COMPLETION_REPORT.md) |
| M9 Production readiness | code + runbooks done · **nothing provisioned** | `83edf36` | [`MILESTONE_9_COMPLETION_REPORT.md`](MILESTONE_9_COMPLETION_REPORT.md) · [`../operations/`](../operations/README.md) |
| M10 Launch cutover | readiness audit done · **cutover is the founder's** | `83edf36` | [`MILESTONE_10_COMPLETION_REPORT.md`](MILESTONE_10_COMPLETION_REPORT.md) · [`LAUNCH_READINESS_CHECKLIST.md`](LAUNCH_READINESS_CHECKLIST.md) |

**2026-09-26 — Trainings catalogue (founder request, data + UI, no schema change):** the header item is now **Trainings** at `/programs` (was "Programme" at `/DataBlueprint-AIVibeCoding`; old paths redirect permanently). Two published trainings, each at `/programs/<slug>`: **Learn Vibe Coding** (`learn-vibe-coding`, half-day masterclass — curriculum in [`CURRICULUM_LEARN_VIBE_CODING.md`](CURRICULUM_LEARN_VIBE_CODING.md), prices RM 500 · Rs 5,000 · USD 200, shown 75% off their originals; participants in Pakistan pay through the local partner, not by card — a founder-directed checkout rule) and **Data Blueprint & AI/Vibe Coding** (`data-blueprint-ai-vibe-coding`, renamed from `ai-powered-product-development`, same row id, its bespoke landing kept). Second round the same day: four-card Investment sections, "Who can take this training", "What you get", participant minimums.

**2026-09-26, later still — flagship Investment card content updated again** (founder chat request, data only, no schema change): Malaysia RM 5,000/RM 2,500 (50% off), Pakistan Rs 200,000/Rs 100,000 (55% off), International USD 4,000/USD 1,000 (75% off) — supersedes the "second round" figures immediately above. Malaysia now shows **two** figures on one card via `regionalPricing.malaysia.options` ("Via HRD Corp" at the undiscounted RM 5,000, "Launch offer" at RM 2,500) — the same `options` mechanism Pakistan used to use for its in-person/online split, which Pakistan no longer needs (now one figure + a note, matching International's layout). ⚠ Two open items for the founder, flagged inline in `prisma/seed-data/courses.ts`: (1) which Malaysia figure is wired to `programme_prices`/checkout — defaulted to the lower RM 2,500 "Launch offer", matching Pakistan's prior online-figure-checks-out precedent; (2) Pakistan's stated "55% OFF" is arithmetically 50% off (Rs 200,000 → Rs 100,000) — the founder's figures are used verbatim, nothing silently corrected.

**2026-09-26, later again — HRD Corp fee brought onto the `/programs` listing card too, and the second option relabelled** (founder chat request): "Launch offer" → **"Without HRD Corp"** (`prisma/seed-data/courses.ts`, cascades to both pages). `CourseCard.tsx` (the `/programs` listing card, `showAllRegions`) previously only ever showed the single checkout figure per region; it now renders a region's `regionalPricing.options` the same way the detail page's `RegionCard` does, so Malaysia's card on `/programs` shows both "Via HRD Corp RM 5,000" and "Without HRD Corp RM 2,500 (RM 5,000, 50% OFF)" — before, that split was only visible on the training's own detail page. Every other region/course card is visually unchanged (the `options` branch is additive; a region with no `options` renders exactly as before). Updated `prisma/seed-data/courses.ts`, `src/shared/marketing/CourseCard.tsx`, `tests/integration/catalogue.test.ts`, `tests/e2e/trainings.spec.ts`; re-seeded `p4tc_dev` and `p4tc_test`. Verified 2026-09-26: tsc clean · Vitest 381/381 · Playwright 67/67 · confirmed at `/programs` and `/programs/data-blueprint-ai-vibe-coding` on the local dev server (`localhost:3100`) — not in production, which does not exist yet (see the header).

**2026-09-26, later again — equal-height card gap fixed portal-wide** (founder-reported on `/programs`: "why so much gap before Duration?"; CSS only, no data or logic change). Cause: cards sharing a CSS-grid row stretch to equal height, and `flex-1` on the card's *summary paragraph* made the shorter card's paragraph box absorb the surplus — a blank area under one sentence (made visible once the flagship card grew taller with its two Malaysia figures). Fix: content stacks naturally and the trailing CTA carries `mt-auto`, so surplus collapses at the card's bottom edge. Applied everywhere the same pattern existed — all 14 `flex-1` uses in `app/` + `src/` were audited: 4 fixed (`CourseCard.tsx`, homepage "Three pathways" in `app/(public)/page.tsx`, "Three routes" in `app/(public)/contact-us/page.tsx`, the mentorship package cards in `ProgrammePricing.tsx`), the other 10 are horizontal-layout or sticky-footer uses and were left alone. Persistence check (founder asked "no in-memory or cache"): nothing here holds state; `/programs` and `/programs/[slug]` are `force-dynamic` and read prices from PostgreSQL per request — no in-memory or cached copy of catalogue data exists. Verified: tsc clean · Vitest 381/381 · Playwright 67/67.

**2026-09-26, evening — founder request: "implement Stripe as payment gateway … production ready … then DigitalOcean server", plus "build deployment framework like eCard".** Planned, **not built**: [`MILESTONE_11_EXECUTION_PLAN.md`](MILESTONE_11_EXECUTION_PLAN.md). Key findings recorded there: Stripe is *already* implemented (M4) and stronger than eCard's; the founder's new key is a **restricted key** (`rk_live_`), which `src/config/env.ts` currently rejects — a one-line payment-config change (decision K1) plus a documented permission set; eCard's deployment framework was read in full and its *controls* are to be ported onto this portal's own runtime model (CI-built Docker image + DigitalOcean Managed PostgreSQL + Caddy), not its PM2/nginx/on-box-Postgres model. ⚠ **Security incident:** the live restricted key was pasted into chat — see the plan's §0; it is stored nowhere and must be rolled (§3 item 6 now covers both pasted keys).

**2026-09-26, night — M11 Phase A BUILT** (founder: *"K1 = yes, all recommendations accepted, start Phase A"* → ADR-046). Three things landed, none of them a deployment: **(1)** `src/config/env.ts` accepts Stripe **restricted keys** (`rk_live_`/`rk_test_`) beside `sk_`, with `stripeKeyMode()`; the minimum permission set (Checkout Sessions write · Refunds write · PaymentIntents/Charges/Balance transactions read) is documented in `.env.example`, `DEPLOYMENT_RUNBOOK.md` §2/§6 and `MONITORING_AND_INCIDENTS.md` §4.6; 18 new unit cases. **(2)** The governed **`deploy/` framework** (13 scripts, compose file, Caddyfile, systemd timers, README) porting eCard's controls onto the portal's runtime model — signed expiring token, root-owned `p4tc-deploy` wrapper, backup + migration sandbox before every migration, auto-rollback on failed health, read-only audit, per-step reports, timeouts, no bypass flags. **(3)** `.github/workflows/release.yml` builds and **proves** the `runtime` and `migrate` images on every `v*` tag (migrates a throwaway PostgreSQL, boots in production mode, requires `/api/health` 200) before pushing; `ci.yml` is callable; `Dockerfile` gains the `migrate` target. Verified 2026-09-26 (locally): `deploy/04-release-gate.sh` ran for real — tsc clean · **Vitest 399/399** · `next build` · **Playwright 67/67 against the production build** · `npm audit` advisory (4 high in production deps, reported §3 item 11, not touched). Every script rehearsed end to end in `--dry-run` with a throwaway config and no server; `start.sh --audit` correctly NO-GO on the four missing preconditions; token signing verified against the server-side verification code, tampering rejected. **Not run yet: `release.yml` itself** — it needs a tag push or a manual dispatch; it is the first time the Dockerfile is built anywhere (§3 item 10). Full account: [`MILESTONE_11_EXECUTION_PLAN.md`](MILESTONE_11_EXECUTION_PLAN.md) §8 · [`../../deploy/README.md`](../../deploy/README.md).

**Last verified state (2026-09-23, superseded above):** `tsc` clean · `next build` clean · Vitest **375 / 375** · Playwright **62 / 62** (WCAG 2.2 AA via axe on every screen).

## 3. Waiting on the founder — answer by number when you return
| # | Decision | Where the recommendation is |
|---|---|---|
| 1 | **M8 §5 A1–A7**: organisations & private cohorts, refund on behalf of a participant, invoices, attendance, account deletion policy, two-person revocation, trainer role | [`MILESTONE_8_EXECUTION_PLAN.md`](MILESTONE_8_EXECUTION_PLAN.md) §5 (A1, A3, A4 are Rule 1 schema items) |
| 2 | **M9 §4 J1–J9 — mostly resolved by M11/ADR-046 on 2026-09-26** (J2 hosting, J3 database, J6 release gate, J5 deferred, J7 backups, J9 key type). **Still open:** **J1** the residency *answer* (law/contract/preference — SGP1 is accepted only if nothing binds; ADR-032 blocks provisioning production data until confirmed) · **J4/K13 the domain** · J7's RPO/RTO figures · **J8** CSP | [`../operations/README.md`](../operations/README.md) J-table · [`MILESTONE_11_EXECUTION_PLAN.md`](MILESTONE_11_EXECUTION_PLAN.md) §5 |
| 3 | **Legal**: lawyer review of Terms / Privacy / Refund drafts (`src/content/legal/*`); Terms §11 lacks certificate validity, fee and "not the earned credential" wording; placeholders such as the SSM number render on the legal pages; then set `LEGAL_DOCUMENT_VERSIONS` to publish | [`LAUNCH_READINESS_CHECKLIST.md`](LAUNCH_READINESS_CHECKLIST.md) §1 · [`MILESTONE_4_COMPLETION_REPORT.md`](MILESTONE_4_COMPLETION_REPORT.md) |
| 4 | **Public copy findings** (no copy was changed): "brought to Malaysia" promise, "25 → 5" claim, three stale "no online payment / policy not published" sentences, "certificate of participation" wording, offers with no end date, dead links to `/certifications` and `/courses` | [`LAUNCH_READINESS_CHECKLIST.md`](LAUNCH_READINESS_CHECKLIST.md) §2, esp. §2.8–2.9 |
| 5 | **Ratifications** still open from earlier milestones: ADR-006 (Better Auth) B1–B3, ADR-015 (email provider), M2/M3 scope acceptance, `WIREFRAME_TO_PRODUCTION_PLAN.md` itself (followed since M3, still "proposed") | [`README.md`](README.md) status column · [`../architecture/ARCHITECTURE_DECISION_REGISTER.md`](../architecture/ARCHITECTURE_DECISION_REGISTER.md) |
| 6 | **Rotate TWO pasted Stripe keys:** the test secret key pasted in chat on 2026-09-21, and the **live restricted key pasted in chat on 2026-09-26** (Stripe Dashboard → Developers → API keys → roll). The live one is urgent — it can move real money. Neither is stored in this repository | [`MILESTONE_11_EXECUTION_PLAN.md`](MILESTONE_11_EXECUTION_PLAN.md) §0 · [`../operations/MONITORING_AND_INCIDENTS.md`](../operations/MONITORING_AND_INCIDENTS.md) §4.6–4.7 |
| 10 | **M11 Phase B — the founder's RED actions, in order** (K1–K16 are answered; Phase A is built): (a) **push a `v*` tag** (or run `Release` manually from GitHub → Actions) so `release.yml` builds and proves the images for the first time — fix anything it reveals before spending on infrastructure; (b) confirm **J1** and choose the **domain (K13)**; (c) DigitalOcean: Droplet + Managed PostgreSQL + Container Registry, DNS; (d) `deploy/10-server-bootstrap-serverscript.sh`; (e) type the env files on the server (staging = test-mode Stripe); (f) GitHub variable `P4TC_REGISTRY` + secret `DIGITALOCEAN_ACCESS_TOKEN`; `gh auth login` on the laptop; (g) `deploy/start.sh --audit --env staging` → GO → deploy staging → test-mode checkout + refund → production | [`../../deploy/README.md`](../../deploy/README.md) §3–4 · [`MILESTONE_11_EXECUTION_PLAN.md`](MILESTONE_11_EXECUTION_PLAN.md) §2 Phase B |
| 11 | **`npm audit --omit=dev` reports 4 high-severity advisories in production dependencies** — `deepmerge-ts <8.0.0` and `mysql2 <=3.23.0` (transitive; `npm audit` says "fix available via `--force`", i.e. a breaking upgrade of the parent). Surfaced by the new gate's advisory step 2026-09-26; not touched. Dependency upgrades are a YELLOW/RED change — triage (is `mysql2` even loaded? the portal uses `pg`) and approve before any go-live | `npm audit --omit=dev` · `npm ls deepmerge-ts mysql2` · `RELEASE_GATE.md` step 5 |
| 8 | **Prices**: figures were revised again 2026-09-26 (later still — see §2); two opens remain — (a) confirm the flagship's Malaysia checkout price is the lower "Without HRD Corp" RM 2,500, not the "Via HRD Corp" RM 5,000, (b) confirm Pakistan's stated "55% OFF" against its own figures (Rs 200,000 → Rs 100,000 computes to 50%); the strike-through offers still have no end date (checklist C14) | `prisma/seed-data/courses.ts` inline comments on the flagship's `pricing`/`regionalPricing` · checklist C10–C14 |
| 9 | **Curriculum decisions**: Learn Vibe Coding C1–C5 (half-day certificate, hands-on option, fee credit, demo repository) and Data Blueprint & AI/Vibe Coding D1–D4 (Day 2 build, quiz or not, keep three pace options alongside "2 days", fee credit) | [`CURRICULUM_LEARN_VIBE_CODING.md`](CURRICULUM_LEARN_VIBE_CODING.md) §7 · [`CURRICULUM_DATA_BLUEPRINT_AI_VIBE_CODING.md`](CURRICULUM_DATA_BLUEPRINT_AI_VIBE_CODING.md) §5 |
| 7 | Small UX calls noted in reports: certificate listing toggle needs a Save click; "Certificate: Revoked" shows no date; Notifications lists every outbox message incl. sign-in emails; CSV has no Excel byte-order mark; two-decimal money for USD | M6, M7, M8 reports "Observations" |

## 4. Working agreements that have held since M1 (keep them)
- **Plan first.** Every milestone: execution plan `.md` with decisions numbered → founder answers by number ("1 = Yes …") → build → completion report → README index row. Rule 1: a schema change is listed column by column in the plan before any migration.
- **Never push, never `git add .`**, stage files by name, bounded commits per milestone, commit messages end with the Claude co-author line.
- **`project-artifacts/mockup` stays untouched** (founder, 2026-09-21) — design reference only; a Vitest boundary test forbids importing it.
- **No sample data in production paths**; the first certificate appears when the first completion is recorded.
- **Business rules are the founder's** — recommend, number, wait. Defaults taken overnight are listed in each plan's "Defaults taken" table for review.
- Reference material at `/Users/mustafaqizilbash/Documents/GitHub/ReferenceMaterial` is read-only and confidential (`docs/REFERENCE_MATERIAL_ACCESS.md`).

## 5. Running it locally (macOS, this machine)
```bash
export PATH="/opt/homebrew/opt/node@24/bin:$PATH"   # Node 24 — required for every command
```
- **Databases:** Homebrew PostgreSQL 16, `p4tc_dev` (the portal) and `p4tc_test` (Vitest/Playwright only). Both must have session timezone UTC (`ALTER DATABASE … SET timezone TO 'UTC'`). `.env.local` holds `DATABASE_URL`, `DATABASE_URL_TEST`, `BETTER_AUTH_SECRET`, `PROFILE_ENCRYPTION_KEY`, `JOBS_SECRET`, Stripe test keys, `LEGAL_DOCUMENT_VERSIONS` (dummy for local). Names in `.env.example`.
- **Migrations:** `npm run db:migrate` (dev) · `DATABASE_URL="$DATABASE_URL_TEST" npx prisma migrate deploy` (test) · `npm run db:seed` on both. Migrations are forward-only and never automatic.
- **Dev server:** `npm run dev` → http://localhost:3100 (Claude's browser pane uses the `portal` entry in `.claude/launch.json`). **Only one `next dev` per directory** — Next 16 holds a lock.
- **Administrator:** `npm run admin:grant -- <email>` after registering through the UI.
- **Stripe locally:** `stripe listen --forward-to localhost:3100/api/stripe/webhook --events checkout.session.completed,checkout.session.expired,charge.refunded,charge.updated,refund.updated` (the `--events` list is required; see `webhook.service.ts` for the exact set).
- **Reminders locally:** `curl -X POST -H "Authorization: Bearer $JOBS_SECRET" localhost:3100/api/jobs/certificate-reminders`.
- **Tests:** `npx tsc --noEmit --incremental false` · `npx vitest run` · `npm run test:e2e` (starts its own server on 3101; stop the preview first). If another `next dev` holds the lock: `npx next build && PLAYWRIGHT_SERVER=start npm run test:e2e`. **All of it in one bounded run, the way a deploy gates it:** `deploy/04-release-gate.sh` (Playwright runs against the production build, so it coexists with a running dev server).
- **Deployment rehearsal (no server needed):** `deploy/README.md` §7.
- **Housekeeping:** `rm -rf .next/types` if `tsc` reports stale route types; keep ≥ 10 GB free on disk (Turbopack caches and Playwright traces have filled it before).

## 6. Map of the code (where to look)
`app/(public)` public pages · `app/(auth)` sign-in/register · `app/account` participant area · `app/admin` administrator area (gate in `layout.tsx`) · `app/api` webhooks, photo, export, health, jobs · `src/modules/{identity,catalogue,commerce,reviews,certificates,reports,notifications,platform}` business logic (repositories take `Db`/`Tx`, audit in the same transaction) · `src/shared/chrome` navigation sources (`site-nav`, `account-nav`, `admin-nav`) · `src/content/legal` legal DRAFTS · `src/config` env validation and health · `prisma/` schema, migrations, seed · `tests/{unit,integration,e2e,helpers}` · `scripts/` admin grant, backup, restore rehearsal · `docs/{architecture,execution,operations,design}`.

## 7. How to resume with Claude
Say what you want and, for anything in §3, give the numbers. A good opening: *"Read docs/execution/PROJECT_STATUS.md. Here are my answers to §3 items 1 and 2: …"* Claude will write or update the plan, wait for approval on any schema item, build, test, report, and update this file.
