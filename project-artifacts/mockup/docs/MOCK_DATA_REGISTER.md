# Mock Data Register — REAL vs FAKED

Kept to meaningful product claims only, per the approval message's
adjustment — not a log of trivial simulated UI behaviour. If it's not
listed here, assume any number, name, or outcome shown on `P01`/`P05`/`P06`
is illustrative placeholder content, not a claim about the real product.

*Updated 2026-08-31 for the P01 redesign (expert-led model). `P05`/`P06`
rows describe the still-unreconciled pre-DR-02 baseline.*

| Behaviour | Status | What's actually happening |
|---|---|---|
| **Practitioner on `P01`, `/trainers` and programme detail pages** (`data/practitioners.ts`, `public/experts/`, `public/books/`) | **REAL** | Genuine person and profile content drawn only from the founder's own résumé, authorized public profiles, and (founder-directed, 2026-08-31) his published bio at yourpartnertechnologies.com — including its exact embedded URLs (five Amazon book links, the Medium frameworks article, YouTube/Spotify podcast channels, eight social profiles) and his own book-cover artwork copied to `public/books/`. Community metrics (40,000+ members, 5,000+ followers, 80+ episodes) are **reproduced as the founder publishes them** — his direction to implement that section constitutes the founder confirmation the earlier Tier-2 caution required, and the page labels them "figures as published by the trainer". Still omitted: current employer/role (`HO-14` — sources conflict). **The in-portal profile page was retired 2026-09-02** (founder direction); the card now links out to the trainer's own published Medium profile, so the biography is read at source rather than restated here. `data/practitioners.ts` retains every field intact. **No practitioner may ever be fabricated, including in fixture data** (DR-02 §7) — that rule is about inventing PEOPLE and is unaffected by the point below. The profile explicitly claims no Academy programme-delivery history until cohorts have really run. ⚠️ **The photograph** (`public/experts/mustafa-qizilbash-v2.jpg`) **is AI-generated/AI-enhanced, of the founder's genuine likeness** — replaced 2026-09-06 with a file whose name matched one already refused elsewhere in the archive under the then-current no-AI-imagery rule. That replacement was flagged to the founder twice before proceeding; he confirmed the face is genuinely his, then — later the same day — changed the portal's policy to permit AI-generated/AI-enhanced imagery portal-wide, which is what makes this the current, policy-compliant state. **The file was later renamed `-v2.jpg`** (same day) after browsers that had visited `/trainers` before the swap kept serving the old cached image indefinitely — `public/` assets aren't fingerprinted, so an in-place content swap doesn't reach already-cached browsers; renaming forces every browser to fetch fresh. Full record in `docs/REFERENCE_MATERIAL_ACCESS.md` §5, `docs/IMAGE_SLOTS.md` rule 2, and the `photo` field's own comment in `data/practitioners.ts` |
| **Programme catalogue** (`data/courses.ts`, `/courses`, `/courses/[slug]`, `P01` preview) | **REAL** | Seven genuine programmes migrated 2026-08-31 from the founder's existing training ecosystem at yourpartnertechnologies.com — real programme names, audiences, learning outcomes, curricula and pedagogy, previously authored by him. Provenance and mapping in [`COURSE_CONTENT_MIGRATION.md`](COURSE_CONTENT_MIGRATION.md) |
| Programme **prices** | **REAL — migrated 2026-09-01** | The founder's own published pricing for all seven programmes, across all three published regions (Malaysia RM · Pakistan Rs. · International USD), recovered from the source's client-side `js/main.min.js` and reproduced verbatim. **These are time-limited launch offers and will date** — the page says so. No checkout is implied; payment is not built (ADR-014/OQ-2), so every CTA is an enquiry. See [`COURSE_CONTENT_MIGRATION.md`](COURSE_CONTENT_MIGRATION.md) "Pricing migration" |
| Programme **dates / scheduled offerings** | **None shown — deliberately** | The source publishes none and none may be invented (DR-02 §4.1). Programmes are the proposition layer; offerings remain State A, which keeps `HO-1` open |
| Rubric fragment on `P01` (`H7`) | **Illustrative — format only** | Mirrors the `K06` wireframe in the approved Mockup Specification (criterion + the four level names). Rebuilt 2026-09-02 as a real four-level scale that **shows one level marked "Awarded"** — a depiction of how a completed assessment looks, not a record of one; the block is labelled as such and **no assessor reasoning was written**. The real rubric is not yet authored; when it is, the fragment must come from it (`MVP_BUILD_SPEC` §11.2 — never fake the rubric) |
| **Reserved photography slots** (`components/ImageFrame.tsx` — 8 across P01, `/about-us`, `/trainers` and the programmes hub/detail) | **Illustrated, not photographed — 2026-09-05** | All 8 slots now show an original abstract SVG illustration (`components/illustrations/DeliveryIllustrations.tsx`) via `ImageFrame`'s new `illustration` prop, rather than the bare dashed "Photograph needed" box. The request that prompted this (2026-09-05) was explicitly framed as "AI-generated images" and explicitly accepted overriding the then-current no-AI-photograph rule — no image-generation tool existed in this environment at the time, so hand-drawn original SVG was substituted, and doing so meant the override was never actually exercised: an abstract line illustration is not a photograph and cannot be mistaken for evidence a cohort has run, so it stayed inside the pre-existing "original graphics" exception. **⚠ 2026-09-06: the underlying rule has since changed** — AI-generated/AI-enhanced imagery is now permitted portal-wide, founder direction (see [`IMAGE_SLOTS.md`](IMAGE_SLOTS.md) rule 2). These 8 slots stay illustrated for now simply because nobody has filled them with a real (or AI) photograph yet, not because one is barred. A slot fills by passing `src` (which takes priority over `illustration`) |
| **Open Graph card** (`app/opengraph-image.tsx`) | **REAL — typographic** | Generated at build from the portal's own wordmark, hero line and positioning facts. Contains no photograph and no metric, so nothing in it can date or overclaim. ⚠ `metadataBase` still points at localhost until a real domain exists |
| Team capability heatmap on `P01` (`H8`) | **FAKED / illustrative** | A static grid of proficiency-scale colours with no underlying data, labelled "Illustrative" in the UI and hidden from assistive tech |
| **Contact enquiry form** (`/contact-us`) | **Inert — and says so** | No backend exists, so the form submits nowhere. It shows a plain "Not connected yet" line rather than a fake success toast; a simulated "thanks, we'll be in touch" would be a prohibited mock success state |
| **Contact details** (`/contact-us`) | **REAL — with a stated gap** | Only genuine channels appear: the founder's LinkedIn, Medium, YouTube and Substack (exact URLs) plus the location from `data/practitioners.ts`. ⚠ **There is no business email, phone, office address or response-time commitment anywhere on the page, because none exists.** None may be invented — a wrong address silently loses real enquiries. Open item: see [`SITE_PAGES.md`](SITE_PAGES.md) |
| **`/about-us` positioning claims** | **REAL — traced, not written** | Every claim is taken from DR-02 §1/§2/§6/§7. Nothing is stated about scale, history, client names, team size, founding date, revenue or accreditation, because none of it is established in any approved source |
| Inert CTAs on `P01` ("Register your interest" · "Talk to us about your team" · "Sign in") | **Inert** | `href="#"` — no destination exists yet (`P19`, auth, and registration are unbuilt). Rendered for journey completeness per mockup convention |
| Nav anchors (`/#programmes` etc.) | **Simulated navigation** | `P24`/`P10`/`P17`/`P15` do not exist; navigation routes to the P01 sections that represent those concepts |
| Diagnostic question sequence (`P05`) | **FAKED** | A fixed, linear list of 10 hand-written questions (`data/questions.ts`). Not adaptive, not branching. Real adaptive/branching logic is production business logic and out of scope for a mockup |
| Diagnostic scoring / result personalisation (`P06`) | **FAKED** | Two canned result fixtures (`data/results.ts`). Which is shown is picked by `selectFixture()` — a visible, trivial rule that exists only to make the walkthrough feel responsive. **Must never be mistaken for or evolved into a real scoring engine.** ⚠ `P06` also still renders retired path/milestone vocabulary — pre-DR-02 baseline, pending separately authorized reconciliation |
| "Compared to your target role" targets (`P06`) | **FAKED** | Three illustrative role profiles in `data/roles.ts` |
| Diagnostic in-progress persistence | **Simulated, approved boundary** | `localStorage` key `mockup:diagnostic:in-progress`, raw answers only, never authoritative, cleared on completion. The one approved localStorage use |
| **Certificate of attempt** (`/diagnostic/result`, 2026-09-05 founder request) | **Wireframe — inert by design** | Shown when `fixture.score >= 90` (an illustrative static field, not a computed score — see the row above). "Request certificate" is a genuinely disabled button, not a fake success state; founder intends Stripe for the real integration. ⚠ Sits in unresolved tension with DR-01 ("one credential, no ladder, no bands") and DR-02 ("earned through applied work, not attendance") — framed here as a distinct, lesser artefact from the real earned credential, but that framing is not itself an approved decision. See the file's header comment |
| **Certificate-of-attempt tiers** — Basic/Associate/Professional/Master, named by question count 10/50/100/200 (`data/questions.ts` `QUESTION_COUNT_TIERS`, 2026-09-05 founder request) | **FAKED for 50/100/200** | Only Basic (10 questions) is real — no question bank exists for the other three. Shown on both the homepage selector and `/diagnostic`'s own idle stage (added 2026-09-06 — see below); clicking one on the homepage navigates to `/diagnostic` instead of doing nothing, but it is still just "(soon)" once you arrive — no tier is actually unlocked by that click. ⚠ Naming a certificate ladder is exactly what DR-01 says the Academy's credential does not have — flagged, not resolved, in both `HomeDiagnostic.tsx`'s and the result page's header comments |
| **`/diagnostic` idle landing stage** (`components/signature/DiagnosticStartCard.tsx`, 2026-09-06 founder direction) | **REAL — structural change** | The standalone diagnostic page previously dropped a visitor straight into question 1 with no context. It now shows the same framing as the homepage's embedded diagnostic first (heading, three honest bullets, tier selector, Start button, Certificate-of-Attempt disclaimer) via a component shared with `components/HomeDiagnostic.tsx`, so the two entry points cannot drift apart. Resuming a genuine in-progress attempt (existing behaviour) still skips straight to the question, unchanged |
| **Certificate-of-attempt "account + USD 10 fee" copy** (2026-09-05 founder request) | **Wireframe — describes an intended flow, nothing built** | No account/authentication system exists anywhere in this codebase. The copy states the intended requirement; it gates nothing |
| **"Cancel test"** (`DiagnosticQuestionCanvas`, both diagnostic entry points, 2026-09-05 founder request) | **REAL** | Genuinely discards the in-progress `localStorage` answers (after a confirm prompt) and returns to idle (homepage) or home (`/diagnostic`). Not a mock — it's real client-side behaviour on non-authoritative, already-approved local state |
| Theme toggle | **Ephemeral, not persisted** | Sets `data-theme` in memory only; deliberately avoids localStorage. A review affordance, not a product feature |
| **HRD Corp Accredited Trainer status** (`data/practitioners.ts` `hrdCorpAccreditation`, `components/HomeHero.tsx` floating card "03", `components/PublicShell.tsx` nav, displayed on `/trainers` — moved there from `/hrd-corp` 2026-09-06 founder direction) | **REAL — trainer only** | Mustafa Qizilbash genuinely holds this HRD Corp accreditation (Trainer ID 68923, valid 08 Jul 2026 – 08 Jul 2029), evidenced by a certificate in the founder's reference archive and confirmed live via HRD Corp's own public verification portal (a founder QR scan, 2026-09-06, returned "Verification Successful"). The certificate PDF and the trainer's passport number are **not** reproduced anywhere in this codebase — only the HRD Corp-issued badge image, plus the Trainer ID and Certificate ID needed to verify independently. See [`REFERENCE_MATERIAL_ACCESS.md`](../../../docs/REFERENCE_MATERIAL_ACCESS.md) §5 |
| **HRD Corp Registered Training Provider status** (`data/hrdCorp.ts`, `/hrd-corp`) | **FAKED-NEGATIVE, stated honestly** | Your Partner Technologies does **not** hold this organisational registration today — an application is in progress (founder statement, 2026-09-06). The page states this as "in progress", not as achieved, and does not use the HRD Corp logo or the phrase "HRD Corp Registered Training Provider" as a present-tense claim, per HRD Corp's own logo-usage terms (reserved for entities that hold the registration) |
| **"HRD Corp Claimable" course status** | **None — explicitly, everywhere** | No course on this site is registered as HRD Corp Claimable. `/hrd-corp` states this plainly; no course card, course detail page, or elsewhere carries a claimable chip. This mirrors the still-open `OQ-8` in the approved specifications (HRD Corp/e-TRIS requirements are general knowledge, not a verified checklist) |
| **"Published work" section on `/trainers`** (2026-09-06 founder direction) | **REAL data, removed from UI** | `lead.books` in `data/practitioners.ts` is untouched — five genuine, published books with real Amazon URLs. Only this page's rendering of them was removed; restoring it is a JSX addition, not a data operation |
| **"Selected delivery" (career achievements) on the `/trainers` lead card** (2026-09-06 founder direction, to match a supplied reference image) | **REAL data, removed from UI** | `lead.careerAchievements` (Hong Leong Bank, PETRONAS, Big-data consulting Pakistan) is untouched in `data/practitioners.ts`. Only this card's rendering of it was removed; same pattern as "Published work" above |

## Binding rule — no licensed imagery (founder direction, 2026-09-02)

**Every image published in this portal must be ours to publish.** No stock
libraries (free tiers included), no third-party licensed photography, no
imagery whose rights sit elsewhere. Acceptable: photographs the founder or
Academy took or commissioned, the founder's own artwork, photographs supplied
with the subject's consent, and original graphics authored for this portal.

This is **broader than the anti-fabrication rule** and catches things it does
not — the DAMA/CDMP material in the reference archive is genuine but
third-party licensed, and may not be reproduced. **When in doubt the slot
stays empty.** See [`IMAGE_SLOTS.md`](IMAGE_SLOTS.md).

## What is real

- The **practitioner** — see the first row. This is the register's most
  important entry: on an expert-led product, the expert is the one thing
  that must never be simulated.
- The design tokens, type scale, and component contract (`components/`,
  `app/globals.css`).
- The screen structure and content hierarchy — `P01` from
  `docs/design/P01_HOMEPAGE_REDESIGN_SPECIFICATION.md`; `P05`/`P06` from
  the (pre-correction) Mockup Specification §4.

## Removed from this register

- *Domain course counts* — the counts themselves were retired from the
  product (DR-02); `data/domains.ts` no longer carries them.
- *Knowledge library article cards* — the three fabricated titles were
  removed from `P01` (omit-rather-than-fill); no article surface remains
  in the mockup.
- *Proof band numbers* — the honest empty-state survives as `H9`; still
  no numbers, real or fake.
