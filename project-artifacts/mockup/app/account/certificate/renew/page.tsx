import { RenewFlow } from "@/components/account/RenewFlow";

/** Certificate renewal (simulated payment) — wireframe, 2026-09-20. */
export default function RenewCertificatePage() {
  return <RenewFlow />;
}
